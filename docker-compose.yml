version: "3"

# Test compose file for building a stachebox stack
services:
  elasticsearch:
    build: 
      context: ./build/elasticsearch
      args:
        ELASTIC_PASSWORD: "5t4ch380x!"
        LOGSTASH_PASSWORD: "L0g5t4ch3!"
    environment:
      - ES_JAVA_OPTS=-Xms1g -Xmx1g
      - http.host=0.0.0.0
      - transport.host=127.0.0.1
      - discovery.type=single-node
      - bootstrap.memory_lock=true

  stachebox:
    build: .
    depends_on:
      - elasticsearch
    ports:
      - "61656:8080"
    environment:
      ENVIRONMENT: production
      ELASTICSEARCH_HOST: elasticsearch
      ELASTICSEARCH_USERNAME: elastic
      ELASTICSEARCH_PASSWORD: "5t4ch380x!"
      STACHEBOX_ADMIN_EMAIL: info@ortussolutions.com
      STACHEBOX_ADMIN_PASSWORD: "5t4ch380x!"
